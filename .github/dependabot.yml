version: 2
updates:
  # Keep GitHub Actions (checkout, lychee, etc.) up to date.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
    labels:
      - ci
      - dependencies
